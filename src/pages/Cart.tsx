import React from 'react';
import styled from 'styled-components';

function Cart() {
  return (
    <Container>
      <span>Cart</span>
    </Container>
  );
}

export default Cart;

const Container = styled.div`
  padding: 30px 10px;
  text-align: center;
`;
